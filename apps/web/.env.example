# === Clerk (auth) ===
CLERK_SECRET_KEY=""
CLERK_WEBHOOK_SECRET=""
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/dashboard
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/dashboard

# === Supabase (database) ===
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""

# === URLs ===
NEXT_PUBLIC_WEB_URL="http://localhost:3000"

# === Rate limiting (Upstash Redis) — optional in dev, REQUIRED in prod ===
# Without these, @repo/rate-limit silently no-ops (no rate limiting).
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# === Security (Arcjet bot/shield protection) — optional ===
# Without this, @repo/security `secure()` early-exits (no bot protection).
ARCJET_KEY=""
